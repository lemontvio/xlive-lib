package avlib

const (
	HLS       = `hls`
	Std       = `std`
	HDL       = `hdl`
	RTMP      = `rtmp`
	ForceUDP  = `forceudp`
	TCP       = `tcp`
	UDP       = `udp`
	TCP_PROXY = `tcp-proxy`
	UDP_PROXY = `udp-proxy`

	// avio
	MEM  = `mem`
	DISK = `disk`
)
